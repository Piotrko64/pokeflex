import { beforeEach, expect, it, vi, describe } from "vitest";

import findTokensLocal from "../_Reducer/helpers/findTokensLocal"; //this function use react-icons

describe("findTokenLocal()", () => {
    it("should return first 3 starting tokens", () => {
        console.log("");
        //if I only import function using react-icons I have error
    });
});
